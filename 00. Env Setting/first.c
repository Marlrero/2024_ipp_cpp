/*
    C Hello World Program
*/

// C Math, I/O Library
#include <stdio.h>
#include <math.h>

// 메인함수 정의
int main() {
    printf("Hello World\n"); // 구문은 세미콜론으로 끝남
    printf("sqrt(4) is %f\n", sqrt(4));

    return 0; // main은 0을 반환
}